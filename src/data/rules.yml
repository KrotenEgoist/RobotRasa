version: "3.1"

rules:

  - rule: low NLU confidence
    steps:
      - intent: nlu_fallback
      - action: utter_rephrase

  - rule: wrong command
    steps:
      - intent: wrong_command
      - action: utter_wrong_command
